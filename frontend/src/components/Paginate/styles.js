import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  pagination: {
    marginTop: "20px",
  },
  h1:{
    color:"red",
    fontSize:"3rem"
  },
  pag:{
    margin:"0 auto"
  }
}));

export default useStyles;
